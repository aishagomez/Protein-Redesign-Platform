<tool name="atom_refine" version="1.0" stage="refinement" active="true">
  <description>Protein structure refinement using ATOMRefine with SE(3)-equivariant graph neural networks and Amber relaxation</description>

  <runtime mode="docker">
    <image>atomrefine:1.0</image>
    <workdir>/app</workdir>

    <command>
      <arg>conda</arg>
      <arg>run</arg>
      <arg>-n</arg>
      <arg>ATOMRefine</arg>
      <arg>sh</arg>
      <arg>refine.sh</arg>
      <arg>{input}</arg>
      <arg>{target_id}</arg>
      <arg>{seq_length}</arg>
      <arg>{runtime_output_dir}</arg>
    </command>

    <resources memory="8g" cpus="2" />

    <env>
      <var name="DGLBACKEND" value="pytorch" />
      <var name="PYTHONUNBUFFERED" value="1" />
    </env>
  </runtime>

  <inputs>
    <param name="input" kind="file" format="pdb" required="true"
           label="Input PDB"
           description="Starting protein model in PDB format" />

    <param name="target_id" kind="string" required="true"
           label="Target ID"
           description="Protein target identifier (e.g., T1062)" />

    <param name="seq_length" kind="int" required="true"
           label="Sequence length"
           description="Protein sequence length" />

    <param name="output_dir" kind="directory" required="false"
           label="Published output directory"
           description="Optional directory where results will be copied" />
  </inputs>

  <outputs>
    <artifact name="refined_models" kind="directory" format="mixed"
              label="Refined models"
              description="Five refined PDB models generated by ATOMRefine" />
  </outputs>
</tool>